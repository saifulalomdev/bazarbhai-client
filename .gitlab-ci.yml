image: node:20

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

before_script:
  - npm install -g firebase-tools

build:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - dist

deploy:
  stage: deploy
  script:
    - firebase deploy --only hosting --token "$FIREBASE_DEPLOY_TOKEN"
  only:
    - main
